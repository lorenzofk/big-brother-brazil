<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Informações do Paredão</title>
    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script src="/js/Chart.js"></script>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/public/css/elimination.css">
    <link rel="icon" href="/public/img/favicon.ico">

    <style>

        .not-found h1 {
            font-size: 50px;
            display: inline-block;
            animation: type .5s alternate infinite;
        }

        @keyframes type{
            from{box-shadow: inset -3px 0px 0px #888;}
            to{box-shadow: inset -3px 0px 0px transparent;}
        }

    </style>

</head>

<body>

<%- include ../../partials/index-navbar %>

<div class="container">
    <div class="album py-5 bg-light">
        <div class="container">
            <div class="row">

                <% if (result.length === 0) { %>

                <div class="col-md-12">
                    <div class="not-found text-center">
                        <h1> Não existem paredões em andamento. :/ </h1>
                    </div>
                </div>

                <% } %>

                <% for(var i = 0; i < result.length; i++) { %>
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm">
                        <img class="card-img-top" src="/public/img/elimination.jpg">
                        <div class="card-body">
                            <hr>
                            <h5 class="card-text"><strong>Nome: </strong> <%= result[i].name %></h5>
                            <h5 class="card-text"><strong>Total de participantes: </strong> <%= result[i].participants.length %></h5>
                            <h5 class="card-text"><strong>Total de votos: </strong> <%= result[i].totalOfVotes.toLocaleString() %></h5>
                            <hr>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a href="/admin/paredao/<%= result[i]._id %>/" class="btn btn-primary btn-sm">Visualizar</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</div>

</body>

</html>